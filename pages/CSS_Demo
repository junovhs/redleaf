<html><head><base href="https://codepen.io/"><title>Dripped Out CSS Classes & Animations Demo</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

  :root {
    --primary-color: #6C63FF;
    --secondary-color: #FF6B6B;
    --tertiary-color: #4ECB71;
    --bg-color: #1A1A2E;
    --text-color: #E0E0E0;
    --card-bg: #16213E;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: 'Poppins', sans-serif;
    background-color: var(--bg-color);
    color: var(--text-color);
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 40px 20px;
    overflow-x: hidden;
  }

  #test-object-container {
    width: 200px;
    height: 200px;
    margin: 20px auto;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  #test-object {
    width: 150px;
    height: 150px;
    background-color: var(--primary-color);
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    font-weight: bold;
    font-size: 1.2rem;
    text-align: center;
  }

  #applied-effects {
    max-width: 800px;
    margin: 0 auto 20px;
    text-align: center;
  }

  #effect-list {
    list-style-type: none;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;
  }

  #effect-list li {
    background-color: var(--card-bg);
    padding: 5px 10px;
    border-radius: 5px;
    display: flex;
    align-items: center;
  }

  #effect-list li button {
    background-color: var(--secondary-color);
    color: white;
    border: none;
    border-radius: 50%;
    width: 20px;
    height: 20px;
    display: flex;
    justify-content: center;
    align-items: center;
    margin-left: 5px;
    cursor: pointer;
  }

  .glowing-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background: 
      radial-gradient(circle at 10% 20%, rgba(108, 99, 255, 0.2) 0%, transparent 20%),
      radial-gradient(circle at 90% 80%, rgba(255, 107, 107, 0.2) 0%, transparent 20%),
      radial-gradient(circle at 50% 50%, rgba(78, 203, 113, 0.2) 0%, transparent 30%);
    filter: blur(40px);
  }

  h1 {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 20px;
    text-align: center;
    background: linear-gradient(45deg, var(--primary-color), var(--secondary-color), var(--tertiary-color));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradientText 5s ease infinite;
  }

  p {
    font-size: 1.2rem;
    margin-bottom: 40px;
    text-align: center;
    max-width: 800px;
  }

  .demo-container {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 30px;
    max-width: 1400px;
    width: 100%;
  }

  .demo-item {
    background-color: var(--card-bg);
    border-radius: 15px;
    padding: 30px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    text-align: center;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .demo-item h3 {
    margin-top: 20px;
    color: var(--primary-color);
    font-size: 1.5rem;
  }

  .demo-item p {
    color: var(--text-color);
    font-size: 1rem;
    margin-top: 10px;
  }

  .demo-item .animation-box {
    width: 120px;
    height: 120px;
    margin: 0 auto 20px;
    background-color: var(--primary-color);
    border-radius: 10px;
    position: relative;
    overflow: hidden;
  }

  .copy-message {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background-color: rgba(0, 0, 0, 0.8);
    color: white;
    padding: 10px 20px;
    border-radius: 30px;
    font-size: 0.9rem;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  @keyframes gradientText {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  @keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.1); }
    100% { transform: scale(1); }
  }
  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    10%, 30%, 50%, 70%, 90% { transform: translateX(-10px); }
    20%, 40%, 60%, 80% { transform: translateX(10px); }
  }
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  @keyframes slideIn {
    from { transform: translateY(50px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
  }
  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
    40% { transform: translateY(-30px); }
    60% { transform: translateY(-15px); }
  }
  @keyframes colorChange {
    0% { background-color: var(--primary-color); }
    33% { background-color: var(--secondary-color); }
    66% { background-color: var(--tertiary-color); }
    100% { background-color: var(--primary-color); }
  }
  @keyframes skew {
    0% { transform: skew(0deg); }
    100% { transform: skew(20deg); }
  }
  @keyframes flip {
    0% { transform: perspective(400px) rotateY(0); }
    100% { transform: perspective(400px) rotateY(360deg); }
  }
  @keyframes wave {
    0% { transform: translateY(0); }
    50% { transform: translateY(-15px); }
    100% { transform: translateY(0); }
  }

  /* Animation Classes */
  .rotate {
    animation-name: rotate;
    animation-duration: 2s;
    animation-timing-function: linear;
    animation-iteration-count: infinite;
  }
  .pulse {
    animation-name: pulse;
    animation-duration: 1s;
    animation-timing-function: ease-in-out;
    animation-iteration-count: infinite;
  }
  .shake {
    animation-name: shake;
    animation-duration: 0.82s;
    animation-timing-function: cubic-bezier(.36, .07, .19, .97);
    animation-iteration-count: 1;
  }
  .fadeIn {
    animation-name: fadeIn;
    animation-duration: 1s;
    animation-timing-function: ease-out;
    animation-iteration-count: 1;
  }
  .slideIn {
    animation-name: slideIn;
    animation-duration: 1s;
    animation-timing-function: ease-out;
    animation-iteration-count: 1;
  }
  .bounce {
    animation-name: bounce;
    animation-duration: 1s;
    animation-iteration-count: infinite;
  }
  .colorChange {
    animation-name: colorChange;
    animation-duration: 5s;
    animation-timing-function: linear;
    animation-iteration-count: infinite;
  }
  .skew {
    animation-name: skew;
    animation-duration: 1s;
    animation-timing-function: ease-in-out;
    animation-direction: alternate;
    animation-iteration-count: infinite;
  }
  .flip {
    animation-name: flip;
    animation-duration: 2s;
    animation-timing-function: ease-in-out;
    animation-iteration-count: infinite;
  }
  .wave {
    animation-name: wave;
    animation-duration: 1s;
    animation-timing-function: ease-in-out;
    animation-iteration-count: infinite;
  }
  .jello {
    animation: jello 2s infinite;
  }
  .heartbeat {
    animation: heartbeat 1.5s infinite;
  }
  .wobble {
    animation: wobble 1s infinite;
  }
  .swing {
    animation: swing 1s infinite;
  }
  .flash {
    animation: flash 2s infinite;
  }
  .rubberBand {
    animation: rubberBand 1s infinite;
  }
  .tada {
    animation: tada 1s infinite;
  }

  /* Additional Classes */
  .rounded { border-radius: 50%; }
  .shadow { box-shadow: 0 0 20px rgba(108, 99, 255, 0.5); }
  .glow { box-shadow: 0 0 20px var(--primary-color); }
  .border { border: 3px solid var(--secondary-color); }
  .gradient { background: linear-gradient(45deg, var(--primary-color), var(--secondary-color)); }
  
  /* New animations and effects */
  @keyframes jello {
    11.1% { transform: none; }
    22.2% { transform: skewX(-12.5deg) skewY(-12.5deg); }
    33.3% { transform: skewX(6.25deg) skewY(6.25deg); }
    44.4% { transform: skewX(-3.125deg) skewY(-3.125deg); }
    55.5% { transform: skewX(1.5625deg) skewY(1.5625deg); }
    66.6% { transform: skewX(-0.78125deg) skewY(-0.78125deg); }
    77.7% { transform: skewX(0.390625deg) skewY(0.390625deg); }
    88.8% { transform: skewX(-0.1953125deg) skewY(-0.1953125deg); }
    100% { transform: none; }
  }
  .jello { animation: jello 2s infinite; }

  @keyframes heartbeat {
    0% { transform: scale(1); }
    14% { transform: scale(1.3); }
    28% { transform: scale(1); }
    42% { transform: scale(1.3); }
    70% { transform: scale(1); }
  }
  .heartbeat { animation: heartbeat 1.5s infinite; }

  @keyframes wobble {
    0% { transform: translateX(0%); }
    15% { transform: translateX(-25%) rotate(-5deg); }
    30% { transform: translateX(20%) rotate(3deg); }
    45% { transform: translateX(-15%) rotate(-3deg); }
    60% { transform: translateX(10%) rotate(2deg); }
    75% { transform: translateX(-5%) rotate(-1deg); }
    100% { transform: translateX(0%); }
  }
  .wobble { animation: wobble 1s infinite; }

  @keyframes swing {
    20% { transform: rotate(15deg); }
    40% { transform: rotate(-10deg); }
    60% { transform: rotate(5deg); }
    80% { transform: rotate(-5deg); }
    100% { transform: rotate(0deg); }
  }
  .swing { animation: swing 1s infinite; }

  @keyframes flash {
    0%, 50%, 100% { opacity: 1; }
    25%, 75% { opacity: 0; }
  }
  .flash { animation: flash 2s infinite; }

  @keyframes rubberBand {
    0% { transform: scale(1); }
    30% { transform: scaleX(1.25) scaleY(0.75); }
    40% { transform: scaleX(0.75) scaleY(1.25); }
    60% { transform: scaleX(1.15) scaleY(0.85); }
    100% { transform: scale(1); }
  }
  .rubberBand { animation: rubberBand 1s infinite; }

  @keyframes tada {
    0% { transform: scale(1) rotate(0deg); }
    10%, 20% { transform: scale(0.9) rotate(-3deg); }
    30%, 50%, 70%, 90% { transform: scale(1.1) rotate(3deg); }
    40%, 60%, 80% { transform: scale(1.1) rotate(-3deg); }
    100% { transform: scale(1) rotate(0deg); }
  }
  .tada { animation: tada 1s infinite; }

  .neonText { text-shadow: 0 0 7px #fff, 0 0 10px #fff, 0 0 21px #fff, 0 0 42px var(--primary-color), 0 0 82px var(--primary-color), 0 0 92px var(--primary-color), 0 0 102px var(--primary-color), 0 0 151px var(--primary-color); }

  .glassmorphism { background: rgba(255, 255, 255, 0.1); backdrop-filter: blur(10px); border-radius: 10px; border: 1px solid rgba(255, 255, 255, 0.2); }

  .neumorphism { background: var(--card-bg); box-shadow: 20px 20px 60px #131836, -20px -20px 60px #192a46; }

  .frostedGlass { background: rgba(255, 255, 255, 0.2); backdrop-filter: blur(5px); border-radius: 10px; }

  @keyframes typewriter {
    from { width: 0; }
    to { width: 100%; }
  }
  .typewriter { overflow: hidden; white-space: nowrap; border-right: 2px solid var(--primary-color); animation: typewriter 4s steps(44) 1s 1 normal both; }

  @keyframes shiny {
    0% { background-position: -100px; }
    20% { background-position: 200px; }
    100% { background-position: 200px; }
  }
  .shiny { position: relative; overflow: hidden; }
  .shiny::after { content: ''; position: absolute; top: -50%; left: -50%; width: 200%; height: 200%; background: linear-gradient(to right, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0.3) 50%, rgba(255, 255, 255, 0) 100%); transform: rotate(30deg); }

  .textStroke { -webkit-text-stroke: 2px var(--primary-color); color: transparent; }
</style>
</head>
<body>
  <div class="glowing-bg"></div>
  <div id="test-object-container">
    <div id="test-object" class="animation-box">
      <span>Test Object</span>
    </div>
  </div>
  <div id="applied-effects">
    <h3>Applied Effects:</h3>
    <ul id="effect-list"></ul>
  </div>
  <h1>Dripped Out CSS Classes & Animations</h1>
  <p>Experience the power of modern CSS with these eye-catching animations and styles. Click on any card to apply its effect to the test object!</p>
  <div class="demo-container">
    <div class="demo-item" data-animation="rotate">
      <div class="animation-box rotate"></div>
      <h3>Rotate</h3>
      <p>Class: rotate</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="pulse">
      <div class="animation-box pulse"></div>
      <h3>Pulse</h3>
      <p>Class: pulse</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="shake">
      <div class="animation-box shake"></div>
      <h3>Shake</h3>
      <p>Class: shake</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="fadeIn">
      <div class="animation-box fadeIn"></div>
      <h3>Fade In</h3>
      <p>Class: fadeIn</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="slideIn">
      <div class="animation-box slideIn"></div>
      <h3>Slide In</h3>
      <p>Class: slideIn</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="bounce">
      <div class="animation-box bounce"></div>
      <h3>Bounce</h3>
      <p>Class: bounce</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="colorChange">
      <div class="animation-box colorChange"></div>
      <h3>Color Change</h3>
      <p>Class: colorChange</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="skew">
      <div class="animation-box skew"></div>
      <h3>Skew</h3>
      <p>Class: skew</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="flip">
      <div class="animation-box flip"></div>
      <h3>Flip</h3>
      <p>Class: flip</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="wave">
      <div class="animation-box wave"></div>
      <h3>Wave</h3>
      <p>Class: wave</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="rounded">
      <div class="animation-box rounded"></div>
      <h3>Rounded</h3>
      <p>Class: rounded</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="shadow">
      <div class="animation-box shadow"></div>
      <h3>Shadow</h3>
      <p>Class: shadow</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="glow">
      <div class="animation-box glow"></div>
      <h3>Glow</h3>
      <p>Class: glow</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="border">
      <div class="animation-box border"></div>
      <h3>Border</h3>
      <p>Class: border</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="gradient">
      <div class="animation-box gradient"></div>
      <h3>Gradient</h3>
      <p>Class: gradient</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="jello">
      <div class="animation-box jello"></div>
      <h3>Jello</h3>
      <p>Class: jello</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="heartbeat">
      <div class="animation-box heartbeat"></div>
      <h3>Heartbeat</h3>
      <p>Class: heartbeat</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="wobble">
      <div class="animation-box wobble"></div>
      <h3>Wobble</h3>
      <p>Class: wobble</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="swing">
      <div class="animation-box swing"></div>
      <h3>Swing</h3>
      <p>Class: swing</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="flash">
      <div class="animation-box flash"></div>
      <h3>Flash</h3>
      <p>Class: flash</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="rubberBand">
      <div class="animation-box rubberBand"></div>
      <h3>Rubber Band</h3>
      <p>Class: rubberBand</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="tada">
      <div class="animation-box tada"></div>
      <h3>Tada</h3>
      <p>Class: tada</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="neonText">
      <div class="animation-box">
        <h3 class="neonText">Neon</h3>
      </div>
      <h3>Neon Text</h3>
      <p>Class: neonText</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="glassmorphism">
      <div class="animation-box glassmorphism"></div>
      <h3>Glassmorphism</h3>
      <p>Class: glassmorphism</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="neumorphism">
      <div class="animation-box neumorphism"></div>
      <h3>Neumorphism</h3>
      <p>Class: neumorphism</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="frostedGlass">
      <div class="animation-box frostedGlass"></div>
      <h3>Frosted Glass</h3>
      <p>Class: frostedGlass</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="typewriter">
      <div class="animation-box">
        <p class="typewriter">Typewriter</p>
      </div>
      <h3>Typewriter</h3>
      <p>Class: typewriter</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="shiny">
      <div class="animation-box shiny"></div>
      <h3>Shiny</h3>
      <p>Class: shiny</p>
      <div class="copy-message">Copied!</div>
    </div>
    <div class="demo-item" data-animation="textStroke">
      <div class="animation-box">
        <h3 class="textStroke">Stroke</h3>
      </div>
      <h3>Text Stroke</h3>
      <p>Class: textStroke</p>
      <div class="copy-message">Copied!</div>
    </div>
  </div>
  
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const demoItems = document.querySelectorAll('.demo-item');
      const testObject = document.getElementById('test-object');
      const effectList = document.getElementById('effect-list');
      
      const animationCode = {
        rotate: `@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
.rotate { animation-name: rotate; animation-duration: 2s; animation-timing-function: linear; animation-iteration-count: infinite; }`,
        pulse: `@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.1); }
  100% { transform: scale(1); }
}
.pulse { animation-name: pulse; animation-duration: 1s; animation-timing-function: ease-in-out; animation-iteration-count: infinite; }`,
        shake: `@keyframes shake {
  0%, 100% { transform: translateX(0); }
  10%, 30%, 50%, 70%, 90% { transform: translateX(-10px); }
  20%, 40%, 60%, 80% { transform: translateX(10px); }
}
.shake { animation-name: shake; animation-duration: 0.82s; animation-timing-function: cubic-bezier(.36,.07,.19,.97); animation-iteration-count: 1; }`,
        fadeIn: `@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
.fadeIn { animation-name: fadeIn; animation-duration: 1s; animation-timing-function: ease-out; animation-iteration-count: 1; }`,
        slideIn: `@keyframes slideIn {
  from { transform: translateY(50px); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}
.slideIn { animation-name: slideIn; animation-duration: 1s; animation-timing-function: ease-out; animation-iteration-count: 1; }`,
        bounce: `@keyframes bounce {
  0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
  40% { transform: translateY(-30px); }
  60% { transform: translateY(-15px); }
}
.bounce { animation-name: bounce; animation-duration: 1s; animation-iteration-count: infinite; }`,
        colorChange: `@keyframes colorChange {
  0% { background-color: var(--primary-color); }
  33% { background-color: var(--secondary-color); }
  66% { background-color: var(--tertiary-color); }
  100% { background-color: var(--primary-color); }
}
.colorChange { animation-name: colorChange; animation-duration: 5s; animation-timing-function: linear; animation-iteration-count: infinite; }`,
        skew: `@keyframes skew {
  0% { transform: skew(0deg); }
  100% { transform: skew(20deg); }
}
.skew { animation-name: skew; animation-duration: 1s; animation-timing-function: ease-in-out; animation-direction: alternate; animation-iteration-count: infinite; }`,
        flip: `@keyframes flip {
  0% { transform: perspective(400px) rotateY(0); }
  100% { transform: perspective(400px) rotateY(360deg); }
}
.flip { animation-name: flip; animation-duration: 2s; animation-timing-function: ease-in-out; animation-iteration-count: infinite; }`,
        wave: `@keyframes wave {
  0% { transform: translateY(0); }
  50% { transform: translateY(-15px); }
  100% { transform: translateY(0); }
}
.wave { animation-name: wave; animation-duration: 1s; animation-timing-function: ease-in-out; animation-iteration-count: infinite; }`,
        jello: `@keyframes jello {
  11.1% { transform: none; }
  22.2% { transform: skewX(-12.5deg) skewY(-12.5deg); }
  33.3% { transform: skewX(6.25deg) skewY(6.25deg); }
  44.4% { transform: skewX(-3.125deg) skewY(-3.125deg); }
  55.5% { transform: skewX(1.5625deg) skewY(1.5625deg); }
  66.6% { transform: skewX(-0.78125deg) skewY(-0.78125deg); }
  77.7% { transform: skewX(0.390625deg) skewY(0.390625deg); }
  88.8% { transform: skewX(-0.1953125deg) skewY(-0.1953125deg); }
  100% { transform: none; }
}
.jello { animation: jello 2s infinite; }`,
        heartbeat: `@keyframes heartbeat {
  0% { transform: scale(1); }
  14% { transform: scale(1.3); }
  28% { transform: scale(1); }
  42% { transform: scale(1.3); }
  70% { transform: scale(1); }
}
.heartbeat { animation: heartbeat 1.5s infinite; }`,
        wobble: `@keyframes wobble {
  0% { transform: translateX(0%); }
  15% { transform: translateX(-25%) rotate(-5deg); }
  30% { transform: translateX(20%) rotate(3deg); }
  45% { transform: translateX(-15%) rotate(-3deg); }
  60% { transform: translateX(10%) rotate(2deg); }
  75% { transform: translateX(-5%) rotate(-1deg); }
  100% { transform: translateX(0%); }
}
.wobble { animation: wobble 1s infinite; }`,
        swing: `@keyframes swing {
  20% { transform: rotate(15deg); }
  40% { transform: rotate(-10deg); }
  60% { transform: rotate(5deg); }
  80% { transform: rotate(-5deg); }
  100% { transform: rotate(0deg); }
}
.swing { animation: swing 1s infinite; }`,
        flash: `@keyframes flash {
  0%, 50%, 100% { opacity: 1; }
  25%, 75% { opacity: 0; }
}
.flash { animation: flash 2s infinite; }`,
        rubberBand: `@keyframes rubberBand {
  0% { transform: scale(1); }
  30% { transform: scaleX(1.25) scaleY(0.75); }
  40% { transform: scaleX(0.75) scaleY(1.25); }
  60% { transform: scaleX(1.15) scaleY(0.85); }
  100% { transform: scale(1); }
}
.rubberBand { animation: rubberBand 1s infinite; }`,
        tada: `@keyframes tada {
  0% { transform: scale(1) rotate(0deg); }
  10%, 20% { transform: scale(0.9) rotate(-3deg); }
  30%, 50%, 70%, 90% { transform: scale(1.1) rotate(3deg); }
  40%, 60%, 80% { transform: scale(1.1) rotate(-3deg); }
  100% { transform: scale(1) rotate(0deg); }
}
.tada { animation: tada 1s infinite; }`,
        neonText: `.neonText { text-shadow: 0 0 7px #fff, 0 0 10px #fff, 0 0 21px #fff, 0 0 42px var(--primary-color), 0 0 82px var(--primary-color), 0 0 92px var(--primary-color), 0 0 102px var(--primary-color), 0 0 151px var(--primary-color); }`,
        glassmorphism: `.glassmorphism { background: rgba(255, 255, 255, 0.1); backdrop-filter: blur(10px); border-radius: 10px; border: 1px solid rgba(255, 255, 255, 0.2); }`,
        neumorphism: `.neumorphism { background: var(--card-bg); box-shadow: 20px 20px 60px #131836, -20px -20px 60px #192a46; }`,
        frostedGlass: `.frostedGlass { background: rgba(255, 255, 255, 0.2); backdrop-filter: blur(5px); border-radius: 10px; }`,
        typewriter: `@keyframes typewriter {
  from { width: 0; }
  to { width: 100%; }
}
.typewriter { overflow: hidden; white-space: nowrap; border-right: 2px solid var(--primary-color); animation: typewriter 4s steps(44) 1s 1 normal both; }`,
        shiny: `@keyframes shiny {
  0% { background-position: -100px; }
  20% { background-position: 200px; }
  100% { background-position: 200px; }
}
.shiny { position: relative; overflow: hidden; }
.shiny::after { content: ''; position: absolute; top: -50%; left: -50%; width: 200%; height: 200%; background: linear-gradient(to right, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0.3) 50%, rgba(255, 255, 255, 0) 100%); transform: rotate(30deg); }`,
        textStroke: `.textStroke { -webkit-text-stroke: 2px var(--primary-color); color: transparent; }`
      };

      const appliedEffects = new Set();

      function updateTestObject() {
        const animationNames = [];
        const animationDurations = [];
        const animationTimingFunctions = [];
        const animationIterationCounts = [];
        const animationDirections = [];

        appliedEffects.forEach(effect => {
          switch(effect) {
            case 'wave':
              animationNames.push('wave');
              animationDurations.push('1s');
              animationTimingFunctions.push('ease-in-out');
              animationIterationCounts.push('infinite');
              animationDirections.push('normal');
              break;
            case 'flip':
              animationNames.push('flip');
              animationDurations.push('2s');
              animationTimingFunctions.push('ease-in-out');
              animationIterationCounts.push('infinite');
              animationDirections.push('normal');
              break;
            case 'skew':
              animationNames.push('skew');
              animationDurations.push('1s');
              animationTimingFunctions.push('ease-in-out');
              animationIterationCounts.push('infinite');
              animationDirections.push('alternate');
              break;
            // Add cases for other animations
            default:
              break;
          }
        });

        testObject.style.animationName = animationNames.join(', ');
        testObject.style.animationDuration = animationDurations.join(', ');
        testObject.style.animationTimingFunction = animationTimingFunctions.join(', ');
        testObject.style.animationIterationCount = animationIterationCounts.join(', ');
        testObject.style.animationDirection = animationDirections.join(', ');

        const nonAnimationClasses = Array.from(appliedEffects).filter(effect => 
          !['wave', 'flip', 'skew' /* Add other animation class names here */].includes(effect)
        );
        testObject.className = 'animation-box ' + nonAnimationClasses.join(' ');
      }

      function updateEffectList() {
        effectList.innerHTML = '';
        appliedEffects.forEach(effect => {
          const li = document.createElement('li');
          li.textContent = effect;
          const removeButton = document.createElement('button');
          removeButton.textContent = '×';
          removeButton.addEventListener('click', () => {
            appliedEffects.delete(effect);
            updateTestObject();
            updateEffectList();
          });
          li.appendChild(removeButton);
          effectList.appendChild(li);
        });
      }

      demoItems.forEach(item => {
        const animationName = item.getAttribute('data-animation');
        
        item.addEventListener('click', function() {
          if (appliedEffects.has(animationName)) {
            appliedEffects.delete(animationName);
          } else {
            appliedEffects.add(animationName);
          }
          updateTestObject();
          updateEffectList();

          const code = animationCode[animationName];
          navigator.clipboard.writeText(code).then(() => {
            const copyMessage = this.querySelector('.copy-message');
            copyMessage.style.opacity = '1';
            setTimeout(() => {
              copyMessage.style.opacity = '0';
            }, 2000);
          });
        });
      });
    });
  </script>
</body>
</html>
